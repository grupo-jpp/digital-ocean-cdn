{
  "fields": {
    "connection": "Connection",
    "bucket": "Bucket (Space name)",
    "cdnEndpoint": "CDN Endpoint",
    "keyPrefix": "Key Prefix"
  },
  "tooltips": {
    "cdnEndpoint": "Optional CDN URL (e.g. https://cdn.example.com). Falls back to the Spaces endpoint.",
    "keyPrefix": "Optional object-key prefix inside the bucket (e.g. 'pim/files')."
  },
  "options": {
    "type": {
      "local": "Local",
      "digitalOceanSpaces": "Digital Ocean Spaces"
    }
  }
}
